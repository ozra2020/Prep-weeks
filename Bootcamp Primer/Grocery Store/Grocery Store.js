var shopper = {
    groceryCart: ["apples", "oranges", "bread", "chicken", "tomato"],
    price: 250,
    list: function(){
        return ("Shopper can take from " + shopper.groceryCart)
        return ("Shopper can spend " + shopper.price)
    }
}
    console.log("Shopper can take from " + shopper.groceryCart)
    console.log("Shopper can spend " + shopper.price)

    
